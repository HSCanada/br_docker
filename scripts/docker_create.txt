# upgrade docker containters
# 2 Feb 18

#proxy

docker stop br_proxy
docker rm br_proxy

docker run \
        --name br_proxy \
        -p 80:80 \
        -v /var/run/docker.sock:/tmp/docker.sock:ro \
        -d jwilder/nginx-proxy

#notebook

cd /home/trevor/repos/docker-stacks

git pull

make build-all


cd /home/br_docker/br_notebook


docker build -t jupyter/br-notebook .


docker stop br_notebook 
docker rm br_notebook 

docker run \
        --name br_notebook \
        -e VIRTUAL_HOST=nb-qa.br.hsa.ca \
        -p 8888:8888 \
        -v /home/notebooks:/home/jovyan/work \
        -d jupyter/br-notebook \
        start-notebook.sh --NotebookApp.token=''


#wiki DB

docker stop br_mysql
docker rm br_mysql 


docker run \
        --name br_mysql \
        -v /home/br_mysql/conf.d:/etc/mysql/conf.d \
        -v /home/br_mysql/initdb.d:/docker-entrypoint-initdb.d \
        -v /home/br_mysql/data:/var/lib/mysql \
        -e MYSQL_ROOT_PASSWORD=booger \
        -d mysql

#wiki app

docker stop br_mediawiki 
docker rm br_mediawiki 

docker run  \
        --link br_mysql:mysql \
        --name br_mediawiki \
        -e VIRTUAL_HOST=wiki.br-qa.hsa.ca \
        -p 8080:80 \
        -v /home/br_mediawiki/config/LocalSettings.php:/var/www/html/LocalSettings.php \
        -v /home/br_mediawiki/images:/var/www/html/images \
        -v /home/br_mediawiki/extensions:/var/www/html/extensions \
        -d synctree/mediawiki


#project DB (tbd)

docker stop br_postgres
docker rm br_postgres


docker run \
        --name br_postgres \
        -e POSTGRES_PASSWORD=secret \
        -e POSTGRES_USER=redmine \
        -p 5432:5432 \
        -d postgres

